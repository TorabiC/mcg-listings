{
  "idx_broker": {
    "enabled": true,
    "credential_env_or_path": "/mnt/user-data/uploads/Second Brain/.secrets/idx-broker-api-key.txt",
    "notes": "IDX Broker Platinum/middleware API. Base URL https://api.idxbroker.com (partner/middleware host historically middleware.idxbroker.com). Auth via 'accesskey' HTTP header (+ optional 'outputtype' header, default json). Key comes from IDX Broker account > My Account > API Access (Platinum Members). Endpoints used: GET /clients/listingstat (per-listing view counts, if plan supports Listing Stats), GET /leads/lead (lead/contact records tied to listing MLS #), GET /clients/featured or /clients/listcomplex/<id> to resolve slug -> MLS#. Set env var IDXBROKER_ACCESSKEY (or point credential file path here) to activate. Missing/blank key => adapter returns zeros with data_quality 'missing', never raises."
  },
  "constant_contact": {
    "enabled": true,
    "credential_env_or_path": "/mnt/user-data/uploads/Second Brain/.secrets/constant-contact-token.json",
    "notes": "Constant Contact API v3 (developer.constantcontact.com). OAuth2 server-side flow (authorization_code with refresh_token, or PKCE) required \u2014 CC has no static API key, only short-lived OAuth access tokens + long-lived refresh token. Store refresh token at path in credential_env_or_path (or env var CONSTANTCONTACT_OAUTH_TOKEN holding a JSON blob {access_token, refresh_token, expires_at}); adapter refreshes access_token via POST https://authz.constantcontact.com/oauth2/default/v1/token when expired. Endpoints used: GET /v3/reports/campaign_tracking_stats/{campaign_activity_id}, GET /v3/emails/activities (search by listing tag/segment) to find campaigns referencing a listing slug. Campaigns are matched to a listing by naming convention (listing slug or address substring in campaign name) \u2014 see collect.py CCAdapter._match_campaigns. All CC actions in this system are READ-ONLY reporting; no sends are ever triggered here. Missing/blank token => zeros + data_quality 'missing'. App 'MCG Seller Reports' client_id/secret live in Second Brain .secrets/constant-contact-api.txt; refresh+access token JSON expected at credential path once OAuth consent completes."
  },
  "ga4": {
    "enabled": true,
    "credential_env_or_path": "/mnt/user-data/uploads/Second Brain/.secrets/ga4-service-account.json",
    "property_id": "533973860",
    "notes": "Google Analytics 4 Data API (Google Analytics Admin required to find property id in GA4 Admin > Property Settings > Property ID, format '123456789', NOT the 'G-XXXX' measurement id). Service account JSON key file (Google Cloud Console > IAM > Service Accounts > Keys) must be added as a Viewer on the GA4 property (GA4 Admin > Property Access Management). Adapter implements JWT-signed OAuth2 token exchange (RS256, scope https://www.googleapis.com/auth/analytics.readonly) against https://oauth2.googleapis.com/token, then POSTs runReport to https://analyticsdata.googleapis.com/v1beta/properties/{property_id}:runReport filtered by pagePath/pageTitle matching the listing's webflow_page URL slug. Set property_id above and drop the service account key at the path in credential_env_or_path to activate. Missing property_id or key file => zeros + data_quality 'missing', never raises."
  },
  "tawk": {
    "enabled": true,
    "credential_env_or_path": "/mnt/user-data/uploads/Second Brain/.secrets/tawk-api-key.txt",
    "notes": "PARTIAL 2026-07-17: property_id 56a434c0942843aa26ec82bf captured; JS API key saved in .secrets/tawk-api-key.txt but tawk REST (/v1/chat.list) returns 401 with it - REST needs a separate tawk API key (Admin > Settings, may be an add-on). Adapter degrades to data_quality 'missing' until a REST key replaces the file contents. tawk.to REST API (Property/Widget level API key from tawk.to Dashboard > Administration > Property Settings > REST API, per-widget). Endpoints used: GET https://api.tawk.to/v3/chat/{chat_id} and GET https://api.tawk.to/v3/property/{property_id}/chats (or /chat/history) with 'Authorization: Bearer <key>'. Chats are matched to a listing by scanning chat transcript/tags for the listing address or slug (see collect.py TawkAdapter._chat_mentions_listing). Set env var TAWK_API_KEY to activate. Missing key => zeros + data_quality 'missing'.",
    "property_id": "56a434c0942843aa26ec82bf"
  },
  "portals": {
    "enabled": true,
    "credential_env_or_path": null,
    "notes": "No API \u2014 manual intake only. Cameron (or future OCR step) drops per-listing JSON/CSV files under intake/<slug>/ containing homes.com (residential) or Crexi/LoopNet (commercial/land) view/save/lead counts for the period. See intake/README.md for exact format. Always 'enabled' in the sense that collect.py always looks for intake files; data_quality is 'live' when a matching-period file is found and parsed, 'missing' when none exists for the period, 'sample' only under --sample."
  },
  "clarity": {
    "enabled": true,
    "credential_env_or_path": "/mnt/user-data/uploads/Second Brain/.secrets/clarity-api-token.txt",
    "token_file": "/mnt/user-data/uploads/Second Brain/.secrets/clarity-api-token.txt",
    "notes": "Microsoft Clarity Data Export API (Project Settings > Setup > Data Export in the Clarity dashboard generates the API token \u2014 project-scoped, no OAuth flow). Endpoint used: GET https://www.clarity.ms/export-data/api/v1/project-live-insights?numOfDays=N with 'Authorization: Bearer <token>'. numOfDays is capped at 3 by the API itself, so a pull can only ever cover the most recent 1-3 days regardless of the reporting window requested \u2014 collect.py treats this as a supplemental engagement signal (sessions/engaged sessions), never a primary/period-complete view count, and folds it into the anonymized 'MCG website analytics' channel alongside GA4 (see generate.py CHANNEL_LABELS). As of 2026-07-30 the credential file at token_file does NOT exist yet (Clarity account/token pending) \u2014 ClarityAdapter.is_configured() returns false in that state and collect.py records data_quality 'missing' with note 'credential pending', never raising. Set credential_env_or_path/token_file to the token once issued.",
    "credential_status": "pending -- file absent as of 2026-07-30"
  },
  "webflow": {
    "enabled": false,
    "credential_env_or_path": null,
    "notes": "No analytics/page-view API. Webflow hosts the MCG site's pages, but 'website traffic' for seller reports is measured through GA4 (+ Clarity supplemental engagement, see the 'clarity' entry above) instrumented on those same Webflow-hosted pages \u2014 GA4/Clarity numbers already ARE Webflow's page-view numbers. There is deliberately no WebflowAdapter in collect.py; writing one would double-count real traffic against a second, fabricated number, which the system's no-fabricated-data policy forbids. This entry exists purely as documentation so a future maintainer doesn't go looking for a Webflow adapter that was never supposed to exist."
  }
}